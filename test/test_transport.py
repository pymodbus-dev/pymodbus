"""Test transport."""
# import pytest

# from pymodbus.transport.base import BaseTransport


class TestTransport:  # pylint: disable=too-few-public-methods
    """Unittest for the transport module."""

    def test_base_properties(self):
        """Test properties."""
        # transport = BaseTransport(None, "test", None)
        # assert not transport.ps_close_comm_on_error

        # with pytest.raises(RuntimeError):
        #     transport.connection_made(None)
